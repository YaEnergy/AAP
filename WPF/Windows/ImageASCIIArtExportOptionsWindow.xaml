<Window x:Class="AAP.UI.Windows.ImageASCIIArtExportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AAP.UI.Windows" xmlns:viewmodels="clr-namespace:AAP.UI.ViewModels"
        mc:Ignorable="d"
        Title="Image ASCII Art Export Options" ResizeMode="NoResize" SizeToContent="WidthAndHeight" MinWidth="250" MinHeight="150" MaxWidth="350" MaxHeight="400">
    <DockPanel Background="{DynamicResource Background}">
        <DockPanel.DataContext>
            <viewmodels:ImageASCIIArtExportOptionsWindowViewModel x:Name="WindowViewModel"/>
        </DockPanel.DataContext>
        <WrapPanel DockPanel.Dock="Top" Margin="10,5,10,5">
            <Label Content="Text Size" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Center" Height="22" Text="{Binding TextSizeText}" Margin="8,0,0,0" Width="40" InputScope="Number" DockPanel.Dock="Right"/>
        </WrapPanel>
        <Button Margin="10,0,10,5" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Content="{Binding CloseButtonContent}" Command="{Binding CloseButtonCommand}"/>
    </DockPanel>
</Window>
